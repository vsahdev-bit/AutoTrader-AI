# =============================================================================
# AutoTrader AI - ML Services Dependencies
# =============================================================================

# Core ML/Data Science
numpy>=1.26.0
pandas>=2.1.0
scikit-learn>=1.3.0
torch>=2.1.0
transformers>=4.35.0

# API Framework
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.4.0

# Database
psycopg2-binary>=2.9.9
asyncpg>=0.29.0            # Async PostgreSQL client
clickhouse-connect>=0.6.0  # Async ClickHouse client
redis>=5.0.1

# Kafka/Streaming
kafka-python>=2.0.2
confluent-kafka>=2.3.0

# LLM/AI (in fallback order: OpenAI → Anthropic → Groq)
langchain>=0.0.340
openai>=1.3.0              # Primary - paid
anthropic>=0.7.0           # Fallback 1 - paid (Claude API)
groq>=0.4.0                # Fallback 2 - free tier available (Llama on Groq)

# HTTP/Async
aiohttp>=3.9.0  # Async HTTP client for news connectors
tenacity>=8.2.0  # Retry logic with exponential backoff

# RSS/XML Parsing
feedparser>=6.0.0  # RSS feed parsing (optional, we use stdlib xml)

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0.1

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
httpx>=0.25.0  # For testing FastAPI endpoints
